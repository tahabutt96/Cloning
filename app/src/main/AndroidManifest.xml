<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.internship.cloning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".taha.DataActivity"></activity>
        <activity android:name=".cardSix" />
        <activity android:name=".ebad.cardFive" />
        <activity android:name=".cardThree" />
        <activity android:name=".cardFour" />
        <activity android:name=".cardTwo" />
        <activity android:name=".taha.cardOne">

        </activity>
        <activity android:name=".splash"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>